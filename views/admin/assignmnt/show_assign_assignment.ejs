
<%- include ('../layout/header.ejs') %>


<div class="col-lg-12 grid-margin stretch-card">
              <div class="card">
                <div class="card-body">
                <div class="d-flex justify-content-between">
                  <h4 class="card-title">Assign assignments</h4>
          
                  <!-- <a href="/assignments/create" style="padding:10px; border-radius:5px" class="btn-success"><i class="fa fa-plus-circle" aria-hidden="true"></i> Add Assignment</a> -->
              
               </div>
                  <p class="card-description">
                    <!-- Add class <code>.table-bordered</code> -->
                  </p>
                  <div class="table-responsive pt-3">
                    
                    <table class="table table-bordered myTable">
                         <thead>
                        <tr>
                          <th>
                            sno
                          </th>
                          <th>Assign Date</th>
                          <th>
                            Chapter name
                          </th>
                          <th>
                            Student name
                          </th>
                          <th>
                            Assignments
                          </th>
                        
                        </tr>
                      </thead>


                      <tbody>
                        <%  
                        i=1; 
                        data.forEach(function(data){ 
                        %>

<tr>
<td><%= i++ %></td>
<td><%= data.added_on %></td>
<td><%= data.chapter_name %></td>
<td><%= data.studentName %></td>
<td><% data1.forEach(function(record) { %>
    <ul>
      <% if (data.student_id === record.student_id) { %>
        <% record.assignmentNames.forEach(function(assignmentName) { %>
          <li><%= assignmentName %></li>
        <% }); %>
      <% } %>
    </ul>

<% }); %>
</td>



</tr>

                        <% }) %>

                      </tbody>
                    </table>
                  </div>
                </div>
              </div>
            </div>

            <%- include ('../layout/footer.ejs') %>
